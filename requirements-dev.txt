# Repo-source development environment: running tests and tools from a checkout.
#
# The editable cadgen below is what every skill's `cadgen` requirement resolves to
# locally. Skills name the DISTRIBUTION unpinned on develop precisely so this install
# satisfies them; the release pins them to the published version at publish time
# (scripts/release/pin-cadgen-requirements.sh).
--editable ./packages/cadgen
--editable ./viewer/moveit2_server

# cadgen's own runtime dependencies (build123d, cadquery-ocp, ezdxf, shapely) come with
# the editable install above. playwright is cadgen's `snapshot` extra, which the skills
# that render ask for; it is listed here so a checkout gets it without the extra syntax.
playwright

# Repo tests only.
networkx
lxml
pytest
trimesh
